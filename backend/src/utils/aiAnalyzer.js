exports.analyzePhoto = async (_photoData) => {
  // Basit bir simülasyon yapıyoruz
  // Gerçek uygulamada bir AI modeli entegre edilebilir
  return {
    outfit: 'default',
    recommendedTag: 'trenchcoat' // Örnek: Hava yağmurlu ise trençkot öneriliyor
  };
};
